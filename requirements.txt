# LeadFund dependencies — pinned for Python 3.11 on Apple Silicon (arm64)
# All pins below ship cp311 macosx_arm64 wheels (no source builds needed).

numpy==1.26.4
scipy==1.13.1
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
jupyter==1.1.1
